# ── 개인 데이터: 절대 깃허브에 올리지 않는다 ──────────────────────────────
# 실제 대출·할부·카드 기록이 들어 있다. git add -f 로도 넣지 말 것.
장부.json
장부*.json
장부.json.backup
*.json.tmp
backups/
# 내보내기로 만든 파일 (실제 금액이 들어 있다)
*.csv
한눈장부_*.json

# ── 만들어지는 파일 ───────────────────────────────────────────────────────
# index.html 은 build.py 가 src/ui.html 에서 만든다. 원본만 올린다.
index.html

# ── 작업 부산물 ───────────────────────────────────────────────────────────
*.bak
*.bak_*
*.log
.claude/
__pycache__/
*.pyc
.DS_Store
Thumbs.db
.venv/
venv/
