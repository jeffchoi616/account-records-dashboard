# 한눈 장부 — 작업 규칙

개인 장부 대시보드. 화면 하나(`src/ui.html`)로 두 곳에서 돈다.

## 1. 개인 데이터는 절대 저장소에 올리지 않는다

`장부.json` 과 `backups/`, 내보낸 CSV 에는 사용자의 실제 대출·카드 기록이 들어 있다.
`.gitignore` 가 막고 있지만 커밋 전에 `git status` 로 항상 확인한다. `git add -f` 금지.

올리는 것: `src/ui.html`, `server.py`, `local-db.js`, `build.py`, 실행 파일, 문서.

## 2. 원본은 `src/ui.html` 하나다

`index.html` 은 `build.py` 가 만드는 파일이다. 직접 고치면 다음 빌드에 지워진다.
화면을 고쳤으면 반드시 `python build.py` 를 돌리고, 아티팩트에도 같은
`src/ui.html` 을 다시 게시한다. 둘이 갈라지면 안 된다.

## 3. 저장소는 갈아 끼우는 부품이다

`useCap(name)` 이 `window.__LOCAL__`(내 PC) 과 `window.claude`(아티팩트) 중 하나를 고른다.
화면 코드는 어느 쪽인지 몰라야 한다. 새 기능이 저장소를 쓰면 양쪽 다 되는지 확인한다.

- 내 PC: `local-db.js` → `장부.json` 한 파일. 저장은 통째로 쓰되 `baseVersion` 으로
  다른 기기가 먼저 쓴 걸 덮지 않는다(409 → 서버 내용을 받아들인다).
- 아티팩트: Claude `db` 능력. 문서 5,000개 한도.

## 4. 고치면 스스로 검산한다

"고쳤습니다"로 끝내지 않는다. 금액·비중 계산이 이 앱의 전부다.

1. 고친다
2. 브라우저로 띄워 **숫자를 손으로 검산**한다 — 연 합계, 이달 합계, 남은 할부,
   회차, 외화 환산. "예외가 없다"와 "값이 맞다"는 다르다
3. 결과를 사용자에게 보고한다

## 5. 설계상 정해 둔 것

- **원화 기호는 `₩` 를 쓰지 않는다.** U+20A9 의 가로줄이 글자 폭 밖으로 넘쳐 뒤 숫자를
  가로질러 취소선처럼 보인다(서체를 바꿔도 마찬가지). `1,234,567원` 으로 적는다.
  달러 `$`, 위안 `¥` 는 멀쩡하므로 그대로 쓴다.
- **환율은 기록할 때 굳힌다.** 저장 시점의 환율을 행에 박아 둔다. 나중에 기준 환율을
  고쳐도 지난 기록은 흔들리면 안 된다.
- **카드는 청구 달로 센다.** 마감일이 4면 6.5~7.4 가 7월 청구다. 명세서에서 기간을
  못 찾으면 저장을 막고 사용자에게 직접 넣게 한다 — 추측해서 넣지 않는다.
- **'완납'은 할부에만 있는 상태다.** 지나간 일시불은 완납이 아니라 그냥 지난 기록이다.
- **기본 기간은 한 달이다.** `S.month` 가 있으면 그 달, 없으면 `S.year` 의 열두 달.
  합계는 전부 `periodSum`/`periodTotal` 로 센다 — 새 화면을 만들 때 연 단위로 세지 말 것.
  달력형 막대만은 늘 그 해 열두 달을 보여 준다(맥락).
- **지출 형식(`pay`·`payCard`)은 모든 기록에 있다.** 카드 명세로 들어온 줄은 그 카드로 굳고,
  기둥 기록은 현금/카드를 고른다. 같은 지출을 기둥과 카드 명세에 둘 다 적으면 두 번
  세어지므로 자동으로 이어 붙이지 않는다 — 어디에 적을지는 사용자가 정한다.
- **편집은 입력 장치로 가른다.** `(pointer: fine)` + 너비 700 이상일 때만 편집.
  창을 작게 띄운 PC 에서도 고칠 수 있어야 하고, 폰·태블릿은 보기 전용이어야 한다.
- **화면에 글자를 늘리지 않는다.** 기호와 숫자로 말하고, 설명 문장은 최소로 둔다.
- **색은 `:root` 토큰에서만 정의한다.** 규칙이나 코드에 색을 직접 박지 않는다.

## 6. 실행

```bash
python build.py     # src/ui.html → index.html
python server.py    # 장부 열기 (--lan 붙이면 폰에서도 보임)
```
